/**
 * Moved to `@cap-flow/ucb` (A0 — shared engine extraction). Re-export shim
 * keeps existing `@/lib/portfolio/lots/edge_cases` import sites working with
 * zero behaviour change.
 */
export * from "@cap-flow/ucb/lots/edge_cases";
