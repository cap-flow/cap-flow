# Golden ledger — aida (aida@gmail.com), аудит 2026-06-08

Account `e32ae00d`, wallet «aida trust» `bafcb962…` (`0xf2d7e803…`). 2 позиции.
Сидер: `apps/api/scripts/seed-aida-golden.mts` (positionKey `aida-2026-06-08:POS-NNN`).

## POS-001 — Fluid (arb), ETH-залог, плечо ~1.95× — **$718.82** (FAIL→FIX)

**Реестр (cost basis trace):**
- transfer_in 718.824102 USDC ($719.11).
- swap `0x91c5dcb9` (0x/RFQ): **out 718.824102 USDC → in 0.304514 WETH**. On-chain receipt
  декодирован: 2 USDC-трансфера (aida→router, router→maker), USDC обратно НЕ возвращался;
  implied WETH = **$2360.56** (ETH сейчас $1683 → ETH упал ~29% за 7 недель).
- swap `0x3cac7415`: WETH→ETH (unwrap).
- lend_supply `0xcfff93e1`: 0.304514 ETH → Fluid; borrow `0x028d3cac`: 350 USDC.

**Cost basis = реально уплаченный USDC = $718.82** (стейбл, надёжно), НЕ receive-side спот
$513.50 (= 0.304514 × текущий $1686). Реальный PnL −$201 (−28%) скрывался как +0.2%.

**Баг (движок):** `position_lot_cost_basis.ts` token→token ветка консумила source-лот ДО
чтения WAC → `wacAt`=null → fallback на market spot → cost терялся. **Fix `a93439a`**
(наследуем `consume().totalCostUsd`). Класс системный: тот же баг был заморожен в golden
artur ETH Fluid ($32,296.72 → $30,953.89).

## POS-002 — GMX V2 (arb), GM-LP — **$196.67** (OK)

**Реестр:** bridge-claim USD₮0 $196.44 → swap → USDC **$196.75** → GMX deposit (yield-deposit
out USDC $196.75) → GM mint $174.15 (market). Cost basis $196.67 ≈ уплаченный USDC (async-
linker), НЕ GM receipt-спот $174.15. PnL −11.4% = реальная стоимость входа GMX. ✓ Корректно
до аудита.

## Маршрутизация (Часть B) — выполнено
- **Движок:** `a93439a` token→token swap cost inheritance + failing-тест.
- **Golden:** обе позиции засеяны (DB + этот ledger); artur ETH Fluid якорь исправлен.
- **Детектор:** `cost_basis_from_spot` (priceSource=fallback) ловит класс POS-001 →
  явный регресс-тест добавлен (`post_port_checks.test.ts`, aida-класс).
- **Знания:** 3-й экземпляр класса «cost basis = market/receipt спот вместо уплаченного».
