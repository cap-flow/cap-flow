# Протокол аудита позиций — с обратной связью во всю систему

> Команда «проведи аудит всех позиций» / «проверь кошелёк». Цель аудита — НЕ
> только пополнить эталоны и найти баги, а **системно улучшать всю платформу**:
> каждый аудит должен оставлять доработки для классификатора, серверного движка,
> детектора аномалий, golden-датасета и знаний для дообучения. Конечная цель
> протокола — чтобы после каждого аудита система становилась точнее и
> самопроверяемее (3× recurrence loop закрывается автоматически).

## Часть A — Ядро аудита (что делаем с КАЖДОЙ позицией)

Поколоночный аудит по 21 столбцу от **реестра операций** (`chain_operations`) +
источников (DeBank/Krystal/Etherscan/Alchemy) + эталонов. 6 слоёв проверки,
per-column вердикт **OK / WARN / FAIL** + root cause + фикс.

**Железные правила (из опыта):**
- Сверять с **реестром операций**, НЕ только с golden/клиентом — иначе пропустишь
  случай «server==client, но оба неверны» (Alice PT-плечо 2026-06-04).
- Cost basis = **сколько реально уплачено** (out-side USDC), НЕ receipt-спот токена.
- Не выдавать вердикт по cost basis без полного **многоходового трейса** (receipt
  → underlying → исходные покупки).
- LP → сверять с Krystar Σ deposit; non-LP → WAC lot-trace.

## Часть B — ⭐ Обратная связь в систему (ОБЯЗАТЕЛЬНЫЙ финальный слой)

Каждая находка (FAIL/WARN) аудита должна быть **смаршрутизирована** в нужный слой
системы и породить **≥1 durable-артефакт**. «Отметили и забыли» — запрещено.

| Тип находки | → Куда дорабатываем | Артефакт |
|---|---|---|
| Операция мисклассифицирована (op_type/notes/protocol неверны) | **Классификатор** | правило/heuristic + classifier-тест |
| Cost basis / PnL неверны, корень в движке | **Сервер/движок** (`@cap-flow/ucb`) | фикс + failing-тест ДО фикса |
| Симптом, который можно ловить автоматически | **Детектор аномалий** | новый чек ИЛИ тюнинг порога + unit-тест |
| Проверенное правильное значение | **Golden-датасет** | эталон в 3 слоя (DB + ledger + память) |
| Паттерн «как считать этот тип позиции» | **Знания для дообучения** | запись в ledger + memory (корм классификатору/детектору) |
| Расхождение источников / провенанс | **Карта источников** ([[capflow_data_source_authority]]) | обновить authority-map |

### Принципы маршрутизации
1. **Ручная находка → детектор обязан научиться ловить её сам.** Если баг нашли
   глазами, но детектор его не видит — это дыра детектора (заводим чек), иначе рецидив.
2. **Новый/изменённый чек детектора → прогнать по аудированному аккаунту → 0
   ложных.** Чек, который шумит на заведомо-корректных позициях — не годен (урок
   `cost_basis_from_spot`: ложно срабатывал на M6 priced-not-trusted; перевели на
   `priceSource==='fallback'`).
3. **Различать «классификатор vs движок vs порт».** Один симптом может иметь корень
   в разных слоях — трейсить до конкретного. Пример: GMX receipt cost basis сломан →
   корень оказался НЕ в классификаторе (notes верны), а в **непортированном на
   сервер `linkAsyncDeposits`** (порт). Маршрут — в движок, не в классификатор.
4. **Golden — это «заморозка правды», детектор — «автопроверка», классификатор —
   «правильный вход».** Аудит кормит все три из одного разбора.

### B.1 — Классификатор в петле: формат «находка → правило → тест»

Классификатор (`apps/api/src/modules/classifier/classifier.ts::doClassify` + зеркало
`apps/web/src/lib/portfolio/classifier.ts` — менять ОБА или выносить в shared) роутит по
`protocol.category` + направлению движений + ролям токенов и выставляет `op_type` +
`notes` (напр. `yield-deposit`/`yield-deposit-fill`). topic0 пока НЕ используется
(апгрейд-план: `classifier-upgrade-plan.md` + словарь `topic0-op-dictionary.md`).

Когда аудит позиции упёрся в операцию — действовать по шагам:

**Шаг 0 — Разграничить корень (КРИТИЧНО, иначе чиним не то).** Достать op из
`chain_operations` и посмотреть фактические `op_type`/`notes`/`raw`:
- `op_type`/`notes`/`category` **НЕВЕРНЫ** (операция попала не в тот бакет / в `unknown`
  / wrapper `execute`/`multicall`/`redeemDelegations` отдал один op_type на мульти-action)
  → **это классификатор** → шаги 1–5.
- `op_type`/`notes` **ВЕРНЫ**, а число ломает движок → **это НЕ классификатор** → маршрут
  в движок/порт (пример: GMX cost basis — notes `yield-deposit-fill` были верны, корень =
  непортированный `linkAsyncDeposits`; чинили движок, классификатор не трогали).

**Шаг 1 — Заморозить «golden-op».** Зафиксировать вход (raw DeBank/on-chain поля: chain,
tx_hash, log_index, protocol, movement, fnName, topic0 если есть) + **ожидаемый выход**
(`op_type`, `category`, `notes`). Это эталон классификатора (аналог golden-позиции, но для op).

**Шаг 2 — Найти дисамбигуирующий сигнал** по приоритету лестницы (из апгрейд-плана):
`topic0` (сигнатура события — надёжнее всего; роутеры `execute`/`multicall` по имени
бессмысленны) → `fnName` → DeBank-type → направление движения. Аудит решает, КАКОЙ сигнал
однозначно классифицирует эту операцию.

**Шаг 3 — Добавить правило** на нужную ступень: либо запись `topic0 → op_type/notes` в
словарь `topic0-op-dictionary.md` + хэндлер, либо heuristic по fnName/движению в `doClassify`.
Менять в **обеих** копиях (api + web). Не плодить тихий `unknown` — fallback по движению.

**Шаг 4 — Юнит-тест классификатора** (`classifier.test.ts`) с замороженным op-фикстуром →
ассерт корректных `op_type`/`category`/`notes`. Test-first: тест краснеет ДО правила, зеленеет
после. Это вечный регресс-гард (закрывает рецидив).

**Шаг 5 — Проверить downstream + не сломать остальное.** Переклассификация меняет данные →
обязательно: (а) перепрогнать аудит затронутой позиции → cost basis/позиция теперь верны;
(б) shadow-diff + golden-replay → 33+ эталона **не хуже** (переклассификация не должна сдвинуть
другие позиции). Если сдвинула — root cause, не маскировать.

**Накопление знаний:** новая сигнатура события → в `topic0-op-dictionary.md` (выверенный
on-chain словарь растёт); паттерн «как классифицировать этот протокол/враппер» → в ledger/память.
Так из аудитов классификатор дообучается системно, а не точечно.

> Текущие кандидаты из `unknown`-бакета (memory): 6 материальных mis-bucketed wrapper'ов
> (`redeemDelegations`×4 +$6828, `execute`×2 −$3464) + 9 UniV3 `collect` — первые цели для
> Шагов 1–5 при следующем аудите.

## Часть C — Worked example: аудит Alice 2026-06-04 (эталон применения Части B)

Один аудит 12 позиций Alice породил доработки во ВСЕХ слоях:

| Находка | Маршрут | Артефакт |
|---|---|---|
| GMX receipt (GLV/GM) cross-protocol cost basis = receipt-спот | Движок | `981c9d0` порт async-linker + проверка реестром |
| PT-залог на занятый стейбл = receipt-спот | Движок | `137141d` borrowed-stable = уплаченный номинал |
| V3: дубликат/пропажа NFT одной пары | Движок | `cc90579` детерминированный матч по nftId (DeBank description) |
| `cost_basis_from_spot` шумит на receipt-декомпозиции | Детектор | `75ca947` чек → `priceSource==='fallback'` (0 ложных) |
| `fee_apr_without_fee` ловит флоат-пыль | Детектор | noise-floor 0.1% |
| 12 позиций выверены от реестра | Golden | ledger `alice-2026-06-04.md` + DB + память |
| Паттерны GMX/Pendle/Avantis/Morpho ops | Знания | разбор в ledger (корм классификатору/детектору) |
| golden-дедуп по неуникальному ключу | Система (БД) | миграция `0032` + обязательный positionKey |

### Кандидаты-чеки детектора, выведенные из этого аудита
- `receipt_collateral_cost_below_paid` — receipt-залог, у которого startUsd <
  Σ(уплаченных стейблов/underlying на закуп) сверх допуска. Ловил бы ОБА cost-basis
  бага, включая случай server==client (по реестру, не по golden).
- `leverage_collateral_borrow_funded` — плечевой залог на заёмные средства:
  проверять, что cost basis = уплаченный номинал, не receipt-спот.

## Часть D — Worked example aida 2026-06-08 + техники поиска НЕОЧЕВИДНЫХ багов

Аудит **одного нового юзера (aida, 2 позиции)** вскрыл **системный баг движка**,
скрывавший −28% убыток, и тот же баг **замороженным в golden artur**. Разбор дал
переносимые проверки/техники — добавляем их в ядро на будущее.

**Что было:** POS-001 Fluid ETH-плечо. Цепочка `transfer_in USDC → swap USDC→WETH →
swap WETH→ETH (unwrap) → lend_supply ETH`. Движок показал cost basis **$513.50** (PnL
+0.2%), реально уплачено **$718.82** (PnL −28%). Корень: `position_lot_cost_basis.ts`
в token→token ветке консумил source-лот ДО чтения его WAC → `wacAt`=null → fallback на
market-спот → реальный cost терялся. On-chain (`0x91c5dcb9`): 718.82 USDC → 0.304514
WETH, USDC назад НЕ вернулся, implied ETH=$2360 (сейчас $1683). Fix `a93439a`.

### D.1 — Новые ОБЯЗАТЕЛЬНЫЕ проверки (добавить в чек-лист Части A)
1. **Swap-баланс (conservation).** Для КАЖДОГО swap: `out_usd ≈ in_usd` в пределах
   fee+slippage (≤~2%). Имбаланс выше → либо DeBank misprice, либо частичный своп →
   **декодировать on-chain receipt** (это и был тычок к багу aida: out $719 vs in $513).
2. **token→token / unwrap хопы.** Цепочки `wrapped→native` (WETH→ETH), `token→token`:
   cost ОБЯЗАН наследоваться от потреблённого лота, НЕ от market-спота на момент свопа.
   Это слепое пятно (стейбл→token ветка работает, token→token — нет).
3. **`priceSource='fallback'` / знак «?».** Перечислить все supplyTokens с fallback — это
   движок САМ пометил cost basis недостоверным = первые цели аудита.
4. **PnL-vs-price-move plausibility.** Плечевая/любая позиция с ~flat PnL, когда базовый
   актив сходил на десятки % за срок удержания, — подозрительно (cost basis ≈ текущий
   спот, а не уплаченное). aida: +0.2% при ETH −28%.
5. **DeBank historical-price sanity.** Сравнить implied-цену из swap-ratio
   (`out_amount/in_amount`) с ценой, которой движок оценил токен; расхождение → DeBank
   price-feed врёт → cost basis якорить к **уплаченному стейблу** (out-side), не к receive-споту.

### D.2 — Техники поиска неочевидных багов
- **On-chain reconciliation (главный арбитр).** При противоречивых DeBank-USD —
  декодировать ERC20 `Transfer`-логи receipt'а (`alchemy ethGetTransactionReceipt` →
  topics from/to, data=amount) для материальных позиций: истинные потоки vs реестр.
  Единственный надёжный способ отличить «misprice» от «partial swap» от «реальной потери».
- **Cross-tracker diff.** `cross_protocol.ts` (SoT) vs `position_lot_cost_basis.ts`
  (popup/lending-override) — параллельные реализации. Отображаемое значение часто из
  НЕ-SoT трекера. Прогнать обе на позиции, флагнуть расхождение (расширить
  `dual_pipeline_equivalence` на token→token + unwrap). aida: SoT давал $719, popup-трекер $513.
- **Signature «сервер прав / клиент нет» + «чинили N раз, возвращается».** → баг на
  call-site (клиент обогащает частичный набор ops / использует другой трекер), а не в движке.
- **Golden provenance check.** Фикс сдвинул golden → НЕ откатывать вслепую: проверить
  провенанс якоря. «pending / skipped / non-verified / engine-frozen» = БЕЗ авторитета →
  фикс прав, обновить golden. artur ETH Fluid: провенанс прямо помечал Fluid «skipped», и
  caveat признавал «reproduces client engine value» (= заморожен, не выверен от реестра).

### D.3 — Где и как чинить (по слоям) + ловушки
- **Число cost basis неверно, а `op_type`/`notes` верны → ДВИЖОК (`@cap-flow/ucb`), НЕ
  классификатор.** (aida: swap/lend_supply классифицированы верно; корень — WAC-наследование.)
- **Параллельные трекеры:** чинить SoT (`cross_protocol`) И popup (`position_lot_cost_basis`),
  либо свести их. Фикс в одном не помогает отображаемому значению из другого.
- **Test-first:** failing-тест с **on-chain-значениями** ДО фикса (вечный регресс-гард).
- **⚠ Ловушка сборки:** `@cap-flow/ucb` экспортит из `dist/` → после правки `src` ОБЯЗАТЕЛЬНО
  `pnpm --filter @cap-flow/ucb build`, иначе web/api тесты И браузер берут СТАРЫЙ код
  (симптом: тест возвращает ровно прежнее число после «фикса»).

### D.4 — Кандидаты-чеки детектора (из этого аудита)
- ✅ **`swap_value_imbalance` УЖЕ реализован** как `swap_movement_imbalance`
  (`registry_checks.ts::findSwapMovementImbalance` + SQL в `admin-tech-audit.service.ts`,
  порог `|out_usd − in_usd|/max > 20%`, подключён). **Подтверждён на aida** (своп
  `0x91c5dcb9`: out $719.11 vs in $513.50 = 28.6% → flagged; явный регресс-тест добавлен).
  ⚠ **ОБЛАСТЬ/ограничение:** ловит только **имбалансные/mispriced** свопы (DeBank misprice /
  partial / недостача leg'а). **Balanced unwrap** (WETH→ETH, 0%, где cost РЕАЛЬНО терялся в
  aida) этим чеком НЕ ловится → его берёт `cost_basis_from_spot` (priceSource=fallback).
  Урок: для класса token→token cost-loss нужны ОБА сигнала (imbalance = lead на misprice,
  fallback = сама потеря cost). Дубль не плодить — расширять область существующего.
- `cost_basis_from_spot` (есть) — подтверждено: ловит token→token-потерю через
  `priceSource='fallback'` (явный регресс-тест добавлен на aida-класс).
- `leverage_flat_pnl_vs_price_move` — плечевая позиция с ~flat PnL при крупном движении
  базового актива за срок (cross-ref hist-цена входа/выхода).
- `tracker_divergence` — `cross_protocol` vs `position_lot_cost_basis` расходятся на позиции
  (авто-версия cross-tracker diff).

## Связанное
[[capflow_position_audit_protocol]] · [[capflow_golden_verification_protocol]] ·
[[capflow_debug_protocol]] · [[capflow_data_source_authority]] ·
[[capflow_anti_recurrence_methodology]] · `notes/golden/alice-2026-06-04.md` ·
`notes/golden/aida-2026-06-08.md`
