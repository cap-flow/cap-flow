# UCB server-port — resume pointer (2026-06-02 EOD)

> Durable handoff. Read alongside `ucb-server-port-master-plan.md` (§B4/§B3-full/§B6
> have turnkey sub-plans) + memory `capflow_ucb_b5_state`. Branch
> `claude/condescending-fermi-99f0ac`, **49 commits ahead of `origin/main`**, tree
> clean, all gated. LOCAL-FIRST — nothing merged/deployed (owner gates the merge).

> ## ✅ UPDATE 2026-06-02 (cont.) — B4 FULLY DONE (slice 2b)
> 5 commits `84cd784`→`c1da985`. The server non-LP opener fetch is live + verified.
> - **2b-pkg** (`84cd784`): pure resolvers → `@cap-flow/ucb/non_lp_opener_resolve`.
> - **2b-fetch** (`8014a80`): `EtherscanClient` stub IMPLEMENTED (proxy-based
>   fetchWalletTokenTransfers/fetchTokenTransfers) + `AlchemyTransfersClient`
>   (getAssetTransfers + block-ts, BASE/avax fallback). etherscan.test 7 +
>   alchemy-transfers.test 4.
> - **2b-source** (`3aaf52b`): `NonLpOpenerSource.forPositions` (targets mirror the
>   client, group by chain+wallet, Etherscan→Alchemy fallback, DefiLlama volatile
>   pricing, fail-soft per-group). non-lp-opener.source.test 6.
> - **2b-wire** (`f88accf`): `computePositions` step 4.8 → `applyNonLpOpenerOverride`
>   (guarded); `nonLpOpenerSource` as a `UcbShadowService` constructor dep; worker.ts
>   DI over the shared upstream proxy.
> - **2b-verify** (`c1da985`): acceptance DI + `ucb-b4-opener-probe.mts`.
>   **LIVE: ucb-shadow-verify 14/0/0; probe map=8 / overriddenCount=7 (fetch FIRES,
>   not a silent no-op); POS-007 GMX GM $5268.32 = $7122.80 USDC × netFrac 0.7396
>   to the cent; async request/fill exercised live.**
> ⚠ Migration 0028 (receipt_token_transfers cache) is an OPTIONAL follow-up — the
> runner fetches live and parity is already proven. **NEXT = B3-full (section B).**

## ⚠️ Worktree trap (read FIRST)
All code lives ONLY in:
`"/Users/vladimir/Desktop/cap-flow (для блокчейна)/.claude/worktrees/condescending-fermi-99f0ac"`
The session shell cwd resets to a DIFFERENT worktree (`funny-tereshkova…`). Every Bash
must `cd "<…>/condescending-fermi-99f0ac" && …` (double-quote the path; don't typo
`vladimir` in Cyrillic). Verify: `ls packages` → `db ucb`.
- After editing `packages/ucb/src/**`: **rebuild the package** (`cd packages/ucb &&
  npx tsc -p tsconfig.build.json`) — web/api vitest resolve `@cap-flow/ucb` from `dist`.
- Workflow subagents were flaky this session (scouts hung ~15min, 0 tool-uses — model
  availability). REVIEW workflows worked. Pragmatic split: scout INLINE (Read, reliable),
  use a REVIEW workflow at the end.

## State check (run these)
```
cd "<…>/condescending-fermi-99f0ac"
git log --oneline -1            # → 37f801a
cd apps/api && npx vitest run src/modules/ucb/         # 61/61
npx tsc --noEmit 2>&1 | grep -c "error TS"             # 36 (pre-existing cex/auth test baseline; 0 from our code)
npx tsx --env-file=../../.env scripts/ucb-shadow-verify.mts   # 14 match / 0 / 0 (last run)
cd ../web && npx vitest run                              # 718/718 ; npx vite build → ✓ ; tsc -p tsconfig.app.json → 250 baseline
```

## Done THIS session (15 commits, 6c94c89→37f801a)
1. **Task #18 — artur ETH Fluid +4.4% FIXED** (`6c94c89`). Root cause: server/web-replay
   called `buildOpenPositions` WITHOUT the methodology → `buildSupplyToken` fell back to
   WAC; threaded `lotMethodology` through. Server LIFO = $32,296.72 to the cent. Live
   verify 14/14.
2. **B6 slice 1 — serving endpoint** `GET /accounts/:id/ucb/positions` (`d50f500`),
   flag `capflow.feature.ucbServerCanonical` (default OFF).
3. **B1 wiring — op-pricing cache-fill job** (`b521994`). Recurring BullMQ job fills
   `op_token_prices` with block-fixed DefiLlama prices (kills sync-time non-determinism).
   Live: 401 prices written, idempotent.
4. **B6 slice 2 — UI adoption** (`12064a0` + hardening `0d2147a`). `useComputedPositions`
   adopts server positions behind the flag with methodology-match + wallet-set guards +
   payload validation; permanent client fallback. **B6 CODE-COMPLETE.**
5. **shadow-diff M5 parity** (`4344926`). Comparator now checks netStartUsd +
   coverageIncomplete + openedAt; new `materialDivergenceCount` (= divergent + clientOnly
   + serverOnly) is the TRUE flip gate.
6. **B4 slice 1** (`7bb3b52`) — extracted `non_lp_opener.ts` + `apply_opener_override.ts`
   into `@cap-flow/ucb`. **B4 slice 2a** (`682c78a`) — extracted `non_lp_cost_basis.ts`.
   All PURE non-LP-opener pieces now shared.

## NEXT (by priority)
### A) ✅ DONE (2026-06-02) — B4 slice 2b+ — server non-LP opener fetch
See the UPDATE banner at the top. Below is the original plan, kept for reference.
- **Slice 2b:** extract the PURE resolvers from `apps/web/src/lib/nonlp/opener_detector.ts`
  (`resolveOpenersFromTransfers`, `resolveOpenerBlocksFromAlchemy` + helpers:
  collectDepositHashes, async request/fill pairing for GMX GLV/GM, WalletTransfer/
  AsyncPairLeg, constants) → `@cap-flow/ucb`. Gated by the 24-test `opener_detector.test.ts`
  real-data fixtures. ⚠ **Server `EtherscanClient.fetchTokenTransfers()` is a THROWING
  STUB** — must implement server Etherscan token-transfer + wallet-transfer + block-timestamp
  fetch (via `integrations/etherscan.ts` / `UpstreamProxyService`) + Alchemy transfers +
  DefiLlama historical, then call the pure resolvers.
- **Slice 3:** migration `0028_receipt_token_transfers` (shared, no user_id, R5 priced_ok).
- **Slice 4:** wire `applyNonLpOpenerOverride` into `ucb.service.ts computePositions`
  (after Krystal V3; guarded no-op when map empty).
- **Verify on-chain:** POS-005 (GLV async), POS-007 (GMX partial-withdrawal $5268.32).
  The shadow-diff M5 `openedAt` field flags this gap → after B4, materialDivergenceCount
  drops for non-LP positions.
- NOTE: the `lendingAudit` half is a client flag (default OFF) → low parity value, defer.

### B) ✅✅ DONE — B3-full — non-Krystal V3 cost basis (Velodrome gauge POS-011)
**FULLY DONE + live-verified (2026-06-02, L1→L3, commits 84cd784-era … ed31054).**
Server reproduces the client's non-Krystal V3 cost basis on-chain:
- L1 override + types → pkg (viem-free, injected resolveDeploymentIds).
- L2a `computeV3CostBasis` + `v3_math` → pkg; L2b-1 `deriveUsdPrices` → pkg.
- L2b-2 **viem added to apps/api** + `v3/positions.fetch.ts` (direct Alchemy admin
  URL: NPM.positions/getPool/slot0/ERC20 multicall + gauge discovery
  alchemy_getAssetTransfers erc721 + ownerOf) + `v3/deployments.ts`.
- L2c `EtherscanClient.fetchV3LiquidityEvents` (getLogs) + `v3/pool-price.fetch.ts`
  (slot0@block-1 + USD anchor); **L2c-fallback `v3/events.fetch.ts`** (Alchemy viem
  getLogs + getBlock times) since Etherscan free can't getLogs on Optimism.
- L2d `V3EnrichmentSource.forPositions` (fail-soft; fetchers injectable).
- L3 wired into `computePositions` step 2.5 (FIRST, before lending; mirrors client)
  + threaded service/worker/acceptance.
- **LIVE:** testakk 14/0/0 preserved (Uniswap V3 enriched → Krystal override wins);
  **POS-011 Velodrome gauge NFT 3427934 = $235.97 to the cent** (`ucb-b3-v3-probe.mts`:
  deposit 0.0269067 WETH + 0.001 WBTC on-chain, mint 0x0ae402fb, slot0 + WETH/USDC
  anchor). Krystal doesn't index it → the server on-chain path is the only source.
- Migration 0027 (v3 cache) OPTIONAL (fetch live; parity proven).

**The entire UCB server port (B1–B6 + B2/B3/B3-full/B4) is now CODE-COMPLETE +
runtime-accepted. Only the owner-gated merge to main remains.**

<details><summary>Original B3-full plan (for reference)</summary>
Heaviest external-API stage.
- **✅ L1 DONE (2026-06-02, `a1ee0a4`):** `applyV3CostBasisOverride` + v3 types
  (V3Position/V3CostBasisResult/V3PositionMap/v3PositionKey) extracted into
  `@cap-flow/ucb` (`v3_types.ts` + `v3_cost_basis_override.ts`). Package stays
  viem-free — the deployment resolver is INJECTED (`resolveDeploymentIds`); the web
  shim passes `findV3Deployments(...).map(d => d.id)`. Web files re-export the types.
  Fixed a latent web no-op (debtTokens.currentUsd → undefined; web tsc 250→249).
  Gate: override 3/3, web 718/718 + build, api ucb 67/67. Override available to the
  server (unwired guarded no-op until L3).
- **✅ L2a DONE (2026-06-02, `c6441bd`):** pure cost-basis aggregation extracted —
  `@cap-flow/ucb/v3_cost_basis_compute.ts` (`computeV3CostBasis`, per-event USD pricing
  INJECTED as `priceForEvent`) + `v3_math.ts` + `V3LiquidityEvent` type. Web
  `use_liquidity_events` hook now delegates to it (single source; audit moved to a
  separate pass), web `math.ts` re-exports. Gate: v3-cost-basis-compute.test 8/8
  (PAXG formula, net, unpriceable, mint-tx, multi-decrease, math), web 718/718, api ucb 75/75.
- **L2b/c (NEXT, the heavy fetch — viem):** ⚠ apps/api has NO viem → add it (already a
  monorepo dep via web). Build a server `v3-enrichment.service` producing `v3PositionMap`
  + `v3CostBasis`:
  - **L2b positions:** port `lib/v3/positions.ts` (`fetchV3PositionsForDeployment` +
    `fetchV3StakedPositions` gauge-staked discovery via `getAssetTransfers` erc721 /
    `fetchMintedNftTokenIds`) using viem `createPublicClient` with the admin Alchemy key
    (`https://{sub}.g.alchemy.com/v2/{KEY}`) — NPM.positions / factory.getPool / pool.slot0 /
    ERC20 symbol+decimals → `V3Position`. Needs `lib/v3/{abis,pool_lookup,math}.ts`.
  - **L2c events+pricing:** Etherscan `getLogs` IncreaseLiquidity/DecreaseLiquidity
    (topic1=tokenId hex) — extend the server `EtherscanClient` with `fetchLogs` (Alchemy
    eth_getLogs fallback); port `historical_pool_price.ts::fetchPoolMintPrice` (pool slot0
    at `blockNumber-1` + USD_ANCHOR_POOLS anchor slot0) → assemble `priceForEvent` →
    feed `computeV3CostBasis`. DefiLlama fallback already on the server.
  - The deployment config (`lib/v3/chains.ts`, viem Chain) → a server config keyed by
    deployment id (npm/factory/anchor pools/alchemy subdomain). `resolveDeploymentIds`
    for the override comes from the same config.
  - Migration `0027` (v3 cost-basis cache) is OPTIONAL like 0028 — fetch live first, prove
    parity, add the cache after.
- **L3:** wire `applyV3CostBasisOverride` FIRST in `computePositions` (server
  resolveDeploymentIds), thread the v3 source through service/runner/worker.
- **Verify:** POS-011 Velodrome gauge **NFT 3427934** (hex 0x344e5e; the "3427422" in
  old notes is a decimal typo) = **$235.97 (slot0, golden) / $237.80 (DefiLlama
  cross-check)**, gauge emissions not pendingFee (R14). Krystal does NOT return this
  Velodrome position → our on-chain path is required. See `capflow_velodrome_gauge_backlog`
  + `capflow_v3_cost_basis` memories.
</details>

### C) Merge to main = prod deploy (OWNER-GATED — needs explicit "да")
`.github/workflows/deploy.yml` auto-deploys on push to `main`. Branch is `[origin/main:
ahead, behind 3]` → rebase the 3 first. All flags default OFF → ships dark (safe). Checklist:
apply migrations 0025–0031 on prod DB FIRST (0031 = users.lot_methodology; + backfill `__migrations` 0017–0024, see A1),
verify worker env keys (Etherscan/Krystal/DefiLlama). Then per-user flag ramp → flip after
shadow-soak shows `materialDivergenceCount === 0` ≥2 weeks.

### D) B6 follow-up (optional): persist per-user methodology server-side
Widens flip coverage (the methodology-match guard already makes it safe, not a blocker).

## Locked rules (owner)
LOCAL-FIRST (main=prod, don't merge/flip without "да"); эталон = реестр (methodology in
`notes/golden/knowledge-base.md §8`); LP→Krystal; **test-first, gate every step**; debug
protocol before any cost-basis bug (ground truth from registry/on-chain, not a guess);
explain in plain language.
